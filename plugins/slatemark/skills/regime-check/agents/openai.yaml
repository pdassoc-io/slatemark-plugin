interface:
  display_name: "Market Regime Check"
  short_description: "Read the cross-asset market backdrop"
  default_prompt: "Use $regime-check to read the current cross-asset backdrop."
dependencies:
  tools:
    - type: "mcp"
      value: "slatemark"
      description: "Slatemark research and user-authored trade journal tools"
      transport: "streamable_http"
      url: "https://slatemark.ai/mcp"
